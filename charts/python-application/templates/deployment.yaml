apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-app-deployment
  namespace: omer
  labels:
    {{ include "pythonApp.lables" . }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{ include "pythonApp.lables" . }}
  template:
    metadata:
      labels:
        {{ include "pythonApp.lables" . }}
    spec:
      containers:
      - name: python-app
        image: {{ .Values.pythonApp.image }}:{{ .Values.pythonApp.tag }}
        ports:
        - containerPort: 8000
